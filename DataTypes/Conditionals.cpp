/*
1. if else if else ---- when using booling boolean expression to make decision --
	e.g. - if (5>6){cout<<"Trule"}
			else {cout<<"False";}

2. Switch - to deal with discrete cases ... -  ----   
`	e.g ---- 
		
    switch (name) {  // switch is used in place of multiple if-else condition
        case 'a' : 
            cout<<"n = a"; 
            break;
        case 'b' : 
            cout<<"n = b";
            break;
        case 'c' :
            cout<<"n = c"; 
            break;
        case 'd' : 
            cout<<"n = d";
            break;
        case 'e' :
            cout<<"n = e";
            break;
         
        default : 
            cout<<"NO such directory"<<endl;
            break;
    }
	break is very necessary think why


3. Ternary Operator -- to shorten the code written in if else condition ternary opaerator is used.
	if (a>b){ x = 5;}  // if a>b is true x will be assigned 5
	else {x = 5}   // if false x = 6 then

	Ternary - int x = (a>b) ? 5:6 /// if a>b is true x will be assigned 5 --- if false x = 6 then


*/